fluidVersion: "0.4.0"
kind: DataProduct
id: compat.legacy.orders_snapshot_040
name: Orders Snapshot Compatibility 0.4.0
description: Minimal fixture used to protect FLUID 0.4.0 backward compatibility.
domain: finance
metadata:
  layer: Bronze
  owner:
    team: legacy-team
    email: legacy@example.com

exposes:
  - id: orders_snapshot
    type: table
    location:
      format: parquet
      properties:
        path: runtime/compat/orders_snapshot_040.parquet
    schema:
      - name: order_id
        type: string
        required: true
      - name: captured_at
        type: timestamp
        required: true
